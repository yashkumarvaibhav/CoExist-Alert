# Demo Snapshots

Bundled stylised demo assets for the CoExist Alert proof of concept.

These files are not field-camera captures. They are local SVG placeholders used by the simulator and seed data so the demo works offline and remains honest. UI surfaces that show them must keep the surrounding feed/channel labelled `SIMULATED`.
